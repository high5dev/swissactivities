/*import Image from 'next/image'

const myLoader = ({ src }) => {
  console.log(src);
  return `https://fra1.digitaloceanspaces.com/contentapi.swissactivities/${src}`
}

const ImageLoader = (props) => {
  console.log(props)
  return (
    <Image
      loader={myLoader}
      src={`https://fra1.digitaloceanspaces.com/contentapi.swissactivities/${props.src}`}
      alt={props.alt}
      width={props.width}
      height={props.height}
      layout={props.layout}
    />
  )
}

export default ImageLoader;*/